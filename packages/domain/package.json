{
  "name": "@kaissi/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Logique métier PURE de Kaissi : monnaie, taxes, remises, totaux, réduction d'événements. Zéro I/O.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "dependencies": {
    "@noble/hashes": "^1.7.1"
  }
}
